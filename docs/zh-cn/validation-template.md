# 手工验证模板

在真实主机上验证防火墙或抓包行为时使用本模板。不要用模拟或 dry-run 结果填写它。

```text
日期：
操作者：
仓库提交：
主机 / VM：
OS / 发行版：
内核 / 版本：
架构：
容器 / namespace 环境：
权限：
  root：
  CAP_NET_ADMIN：
  CAP_NET_RAW：
后端 / 方法：
配置文件：
执行命令：
预期结果：
实际结果：
日志：
初始化前防火墙规则：
init 后防火墙规则：
allow 后防火墙规则：
cleanup 后防火墙规则：
抓包说明：
回滚 / 清理：
是否可复现：
结论：
```
